# Public runtime config only (safe to expose)
PUBLIC_API_PROXY_URL=https://arcraided-api-proxy.<your-subdomain>.workers.dev
PUBLIC_ARC_PROVIDER=arc

# Future public proxy URLs (optional)
# PUBLIC_XYZ_PROXY_URL=
